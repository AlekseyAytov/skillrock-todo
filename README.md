При запуске использовать флаги:
  -a string
        host:port of target HTTP address (default ":8091")
  -d string
        connection string to database (default "postgres://{user}:{password}@{hostname}:{port}/{database-name}")